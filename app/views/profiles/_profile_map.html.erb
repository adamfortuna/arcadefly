<li class="<%= cycle("even","odd")%> clear">

  <span style="width:280px;" class="left clear">
    <%= link_to(profile_map.display_name, profile_path(profile_map)) %>
  </span>
  
  <span style="width:50px;" class="right">
    <a href="#" onclick="profile_group.showMarker('<%= profile_map.permalink %>');return false;">Show »</a>
  </span>
  
  <div class="clear soft">
  	<div class="left">
      <%= #render :partial => 'profiles/favorite', :locals => { :show_text => false, :arcade => arcade } if logged_in? 
      %>
      <p><%= profile_map.address.shortest_line -%></p>
  	</div>
  	
  	<div class="right">
      <% if current_session.addressed_in? -%>
        <%= profile_map.address.distance_to(current_session.address).round_to(2) %> miles away<br />
      <% end %>
      <%= link_to pluralize(profile_map.favoriteships.size, "game"), profile_games_path(profile_map), :class => 'games' %> 
      <%= link_to pluralize(profile_map.frequentships.size, "favorites"), profile_arcades_path(profile_map), :class => 'users' %>
    </div>
  </div>
</li>