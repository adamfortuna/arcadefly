<% cache ({:page => params[:page] || 1, :user_id => session[:user] || 0, :search => params[:page] || ''}) do %>
<ul class="data<%= " logged_in" if logged_in? %>">
  <%= render :partial => 'games/game', :collection => games %>
</ul>

<%= will_paginate games %>
<% end %>